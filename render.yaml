services:
  - type: web
    name: negamail-chess
    runtime: python
    repo: https://github.com/arafat01rahman/NegaMail-Chess
    branch: main
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    buildCommand: |
      echo "🔧 Installing build tools..."
      apt-get update
      apt-get install -y g++ make
      echo "🔨 Compiling chess engine..."
      g++ -O2 -std=c++17 \
        main.cpp \
        board.cpp \
        evaluate.cpp \
        search.cpp \
        uci.cpp \
        -o engine
      chmod +x engine
      echo "✅ Engine compiled and made executable!"
      echo "📦 Installing Python dependencies..."
      pip install -r requirements.txt
      echo "✅ Ready to deploy!"
    startCommand: gunicorn app:app --workers 2 --timeout 30   
    healthCheckPath: /health